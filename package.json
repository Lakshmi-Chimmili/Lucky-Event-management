{
  "name": "lucky-events-management",
  "version": "1.0.0",
  "description": "Lucky Events - Event Management Platform",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npm install",
    "start": "node backend/server.js",
    "seed": "cd backend && npm run seed"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
